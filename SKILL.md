---
name: wonky-handpainted-illustration
description: Generate or edit cohesive characters, achievement badges, app icons, and illustrations in a cute-but-not-childish wonky hand-painted style. Use when the user wants strongly simplified, deliberately awkward flat shapes, opaque gouache texture, or a reusable visual family across app assets.
---

# 粗拙手绘插画

生成一套“造型稚拙、明显概括、可爱但不幼稚”的平面手绘视觉。首要目标是用简单形状重新设计主体；肌理与局部明暗丰富画面，但不能代替造型风格或主导真实体积还原。风格服务于主体识别，不得把任何示例角色、物种或品种固化为默认形象。

## 绝对不可妥协的要求（风格硬门槛）

以下要求适用于所有主体与资产模式，必须进入实际生成提示词并用于生成后验收，不能降级为“尽量”。用户当前明确指令优先；若与硬门槛直接冲突，应说明冲突，不能一边照做一边宣称仍符合本风格。

1. **造型本身必须稚拙、明显概括。** 用少量不规则大形重新组织比例和连接，保留不等宽的弧段、轻微偏心和不完全顺滑的接合。概括不等于几何规整：不得把形状修成匀称曲线、重复锯齿或整齐的剪纸花边，也不靠全轮廓均匀抖动伪装手画。去掉纹理、阴影与表面细节后，仍须看得出有意设计的笨拙感；仅在自然比例或精确轮廓上增加粗糙笔触，判定不通过。
2. **不得精细描摹或追求拟实还原。** 不完整交代解剖、透视、结构转折与真实材质。细节仅用于必要识别，允许省略、合并和符号化；毛发、羽毛、褶皱等可以出现，但不得靠密集、连续的细节构建主体。
3. **必须先有完整、厚实的色块，再有笔触。** 使用有覆盖感的不透明平面色面，允许局部露底、叠擦和轻微错位。笔触以颜料叠擦为主，方向、长短和疏密自然变化，局部可顺形，但不得在整个形体上连续梳理表面、解释生长方向、曲率或体积；不得用碎笔触拆散大形，也不得用全图统一噪点代替手绘。
4. **明暗和肌理不得主导写实塑形。** 允许局部深浅、叠色、接触暗部和丰富的颜料肌理，它们应从属于概括的大形。不建立完整连续的光照、曲面渐变或真实反射体系，不使用精确光滑的矢量边缘；也不为追求扁平而抹掉所有深浅变化，或把画面机械处理成几块均匀色纸。
5. **身份识别必须保留，但不等于锁定真实比例。** 保留主体类别、关键识别关系及用户明确锁定项；其余形体可以压缩、拉长、合并和重新安排。不得擅自将参考图的全部轮廓与结构列为不可改内容，也不得把风格参考的物种、配色、道具或构图复制为默认模板。
6. **神态与动作必须服从内容，不能被风格固定。** 神气、生气、开心、安静等均可；统一的是简练而鲜明的表达方式。不得强制发愣、淡定或固定姿态，也不靠巨眼、腮红或婴儿化比例替代造型设计。

**判定规则：任一硬门槛未满足，即为风格未通过；不能用颜色、纸感、纹理或主体识别正确来抵消。** 先修失败的造型层，不用叠加肌理补救。局部调整不得锁住失败图的结构；整体失败按形体重构处理。若本轮未继续生成，应明确报告未通过项，不宣称已达到目标风格。

## 内置风格锚点

生成时将 [style-reference.png](assets/style-reference.png) 作为默认风格参考。用户明确指定其他风格参考时，以用户指定为准，避免混用冲突的锚点。生成时实际传入所选风格图，只借鉴概括的造型、厚实色块、颜料笔触和有意保留的笨拙感；不得复制示例的物种、神态、配色、道具或构图。始终把用户提供的身份参考与该风格参考分开标注和使用。

## 开始前

先记录以下变量。不得在变量未入表时直接生成；缺失项可从用户上下文或参考图合理推断并明确标注假设，不适用项写“无”：

- `模式`：角色形象 / 成就徽章 / APP Icon / 单幅或场景插画
- `主体`：角色、物件或概念
- `物种/类别`：动物物种或非动物对象类别
- `品种/子类`：影响外形的细分类；不适用时也需明确为“无”
- `神态与动作`：随内容确定，可神气、生气、开心、安静等；不从风格参考继承固定表情
- `身份识别点`：少量足以识别主体的形状、主花纹、颜色或道具；不要把照片中的所有结构和细节都列为必须保留
- `用户明确锁定项`：仅记录用户明确要求保持的比例、结构、花纹或其他特征；未指定则写“无”
- `形体方案`：用 2–5 个不规则大形说明主体如何重构，以及省略哪些结构细节；不能只写“简化、手绘”
- `用途与交付`：画布比例、真实目标像素尺寸、导出格式、背景实色/透明需求、平台规则与是否需要系列化

若用户给了参考图，先区分“身份参考”与“风格参考”。只提取用户要求的内容，不把参考图中的背景、构图或无关细节带入成品。

## 工作流

1. 阅读 [design-language.md](references/design-language.md)，建立共同风格约束。
2. 只阅读当前模式对应的 [asset-modes.md](references/asset-modes.md) 小节。
3. 先确定形体方案：即使暂时去掉肌理和明暗，也应读出稚拙的设计；再从 [prompt-recipes.md](references/prompt-recipes.md) 选择模板，填满变量后再调用图像生成或编辑工具。编辑已有图像时必须包含目标图；有身份参考图时也必须包含。
4. 生成后先逐项检查上面的六条硬门槛，再按 [qa-checklist.md](references/qa-checklist.md) 检查模式适配与交付。
5. 局部修正时只改变一个视觉变量。若用户反馈整体太精致或拟实，按“形体重构”整体重画，联动调整比例、轮廓、结构细节和塑形笔触；仅锁定用户已认可内容，不锁住失败图的造型。

## 系列一致性

多资产输出共享造型逻辑、色彩关系、纹理密度与夸张尺度，构图、神态和动作可因内容与用途而异。
